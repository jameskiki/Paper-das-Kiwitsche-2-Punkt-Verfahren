# AI Context — Das Kiwitsche 2-Punkt-Verfahren

This file is the primary reference for any AI agent (or human developer) who
branches, extends, or reviews this repository. Read this **before** making any
changes.

---

## Purpose

This is a scientific publication repository. The goal is to eventually produce a
peer-reviewable paper on the *Kiwitsche 2-Punkt-Verfahren* (KZV) — a procedure
for estimating PID control parameters for plants that are well approximated by
a first-order-plus-dead-time (FOPDT) model (e.g., thermal / cooling systems),
using Bang-Bang (two-point) control as the initial excitation signal.

### Scope and positioning

The KZV is **not** a replacement for established relay-feedback auto-tuners
such as Åström & Hägglund's method. It is a **specialised variant** of the
same underlying idea (extract plant information from a relay-induced limit
cycle), with a different trade-off:

- **Åström relay method:** extracts a single Nyquist point ($K_u$, $\omega_u$)
  via describing-function analysis. Makes essentially no assumption about the
  plant structure → broadly applicable, but does not separate dead time from
  lag.
- **KZV (this work):** extracts the full FOPDT triple ($\hat K$, $\hat T$,
  $\hat L$) via time-domain slope measurements within each half-cycle.
  Recovers more information from the same experiment, but **only valid when
  the FOPDT approximation holds** and is more sensitive to measurement noise
  (slopes amplify noise).

When writing or editing the paper, frame the KZV as **complementary** to
Åström's method, not as universally superior. Claims should be scoped to
FOPDT-like (notably thermal) processes. Avoid wording that suggests the KZV
outperforms relay feedback in general.

---

## Tech Stack

| Layer | Tool | Notes |
|-------|------|-------|
| Paper | LaTeX (pdflatex + BibTeX) | Source lives in `paper/` |
| Code | Python 3.10+ | Source lives in `python/` |
| Figures | Matplotlib / NumPy | Saved to `paper/figures/` |
| Package management | pip + `requirements.txt` | No conda, no poetry |

---

## Conventions

### LaTeX

- Master document: `paper/main.tex`
- Each logical section is a separate `.tex` file under `paper/sections/` and
  included via `\input{sections/<name>}` in `main.tex`.
- All figures referenced in the paper are generated by Python and stored under
  `paper/figures/`. **Never commit hand-drawn or manually edited figures.**
- Bibliography entries go in `paper/bibliography.bib` (BibTeX format).
- Citation style: IEEE (or similar engineering journal style) — update
  `main.tex` if the target journal changes.

### Python

- All modules live under `python/`.
- Sub-packages: `simulation`, `analysis`, `plotting`.
- Each sub-package has an `__init__.py` that exports the public API.
- Scripts are runnable as modules: `python -m simulation.bang_bang_control`.
- Figures are saved with `matplotlib.pyplot.savefig` to `../paper/figures/`
  (relative to `python/`). Use `dpi=300` and `bbox_inches='tight'`.
- Use type hints and docstrings on all public functions.
- No hardcoded magic numbers — put constants at the top of each module or in
  a shared `config.py` (create when needed).

### Naming

- Variables follow the paper's notation: `K_p`, `T_i`, `T_d`, `T_u`, `A_u`,
  `K` (plant gain), `T` (time constant), `L` (dead time).
- Physical units are always documented in docstrings (SI units preferred).

---

## Scientific Background

### Bang-Bang Control

A two-point (Bang-Bang) controller switches the manipulated variable between
two fixed levels (e.g., fully ON and fully OFF). For a stable plant with
sufficient gain and phase delay, this produces a *limit-cycle oscillation*
around the setpoint.

### Key Measured Quantities

| Symbol | Meaning |
|--------|---------|
| `T_u` | Oscillation period of the limit cycle [s] |
| `A_u` | Peak-to-peak amplitude of the controlled variable |
| `d`   | Switching hysteresis of the Bang-Bang controller |
| `M`   | Relay amplitude = (U_max - U_min) / 2 |

### FOPDT Identification (Transient Slope Method)

The KZV uses the step-response segments embedded within each limit-cycle
half-cycle to identify all three FOPDT parameters:

1. **Dead time L** — sign-change of `dy/dt` after a rising switch occurs at `t_sw + L`
2. **Time constant T** — at `t_sw + L` after a falling switch (with `U_min=0`):
   `T = y_L / |dy_L/dt|`
3. **Gain K** — from the rising switch at `t_sw + L`:
   `K = (T * slope_up + y_L) / U_max`

This is implemented in `analysis/parameter_estimation.py::identify_fopdt_from_transients`.

### PID Synthesis

Uses IMC-based formulas applied to the FOPDT model:
```
K_p = T / (K * (λ + L))
T_i = T
T_d = L / 2
```
Target closed-loop time constant `λ = L` (default).

---

## Roadmap / Open Tasks

### Completed
- [x] Derivation of FOPDT identification formulas (theory section)
- [x] FOPDT simulation validation on benchmark FOPDT plant
- [x] Noise robustness analysis (discussion section)
- [x] Results and discussion sections
- [x] Nonlinear plant extension (Section 5)

### Future Work
- [ ] Experimental validation on a physical cooling test bench
- [ ] Extension to second-order identification using multi-frequency relay tests
- [ ] Online (recursive) adaptation of PID parameters
- [ ] Integration with auto-tuning firmware for industrial controllers

---

## Branch Strategy

| Branch | Purpose |
|--------|---------|
| `main` | Stable, reviewed content only |
| `copilot/*` | AI agent working branches |
| `feature/*` | Human feature branches |

AI agents should always work on a `copilot/<short-description>` branch and open
a pull request against `main`.

---

## Notes for Future AI Agents

1. **Do not change the paper's scientific claims** without explicit user
   instruction.
2. **Do not overstate the method.** The KZV is a specialised FOPDT-oriented
   variant of relay-feedback identification, not a general-purpose
   replacement for Åström's method. Keep comparisons balanced and scope
   claims to plants where the FOPDT assumption is reasonable.
3. **Python changes must not break existing figure outputs** — run the plotting
   scripts and verify figures before committing.
4. **LaTeX changes must compile cleanly** — run `pdflatex main.tex` and check
   for errors.
5. **Keep this file up to date** — if you add a new convention, module, or
   design decision, document it here.
6. Read `CONTRIBUTING.md` for commit-message and PR conventions.
